Incluye dos scripts principales:

- `test_llmstudio.py` → Prueba básica del modelo local.
- `gradio_basic_rag.py` → Sistema básico de RAG con interfaz gráfica.

Instala el archivo `requirements.txt` con `pip install -r requirements.txt`